naftiko: 1.0.0-alpha2
info:
  label: Windmill API — trigger
  description: 'Windmill API — trigger. 2 operations. Lead operation: Cancel All Suspended Jobs for a Specific Trigger. Self-contained
    Naftiko capability covering one Windmill business surface.'
  tags:
  - Windmill
  - trigger
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WINDMILL_API_KEY: WINDMILL_API_KEY
capability:
  consumes:
  - type: http
    namespace: windmill-trigger
    baseUri: ''
    description: Windmill API — trigger business capability. Self-contained, no shared references.
    resources:
    - name: w-workspace-trigger-trigger_kind-cancel_suspended_trigger_jobs-trigger_path
      path: /w/{workspace}/trigger/{trigger_kind}/cancel_suspended_trigger_jobs/{trigger_path}
      operations:
      - name: cancelsuspendedtriggerjobs
        method: POST
        description: Cancel All Suspended Jobs for a Specific Trigger
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: trigger_kind
          in: path
          type: string
          description: The kind of trigger
          required: true
        - name: trigger_path
          in: path
          type: string
          description: The path of the trigger (can contain forward slashes)
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: w-workspace-trigger-trigger_kind-resume_suspended_trigger_jobs-trigger_path
      path: /w/{workspace}/trigger/{trigger_kind}/resume_suspended_trigger_jobs/{trigger_path}
      operations:
      - name: resumesuspendedtriggerjobs
        method: POST
        description: Resume All Suspended Jobs for a Specific Trigger
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: trigger_kind
          in: path
          type: string
          description: The kind of trigger
          required: true
        - name: trigger_path
          in: path
          type: string
          description: The path of the trigger (can contain forward slashes)
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.WINDMILL_API_KEY}}'
  exposes:
  - type: rest
    namespace: windmill-trigger-rest
    port: 8080
    description: REST adapter for Windmill API — trigger. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/w/{workspace}/trigger/{trigger-kind}/cancel-suspended-trigger-jobs/{trigger-path}
      name: w-workspace-trigger-trigger-kind-cancel-suspended-trigger-jobs-trigger-path
      description: REST surface for w-workspace-trigger-trigger_kind-cancel_suspended_trigger_jobs-trigger_path.
      operations:
      - method: POST
        name: cancelsuspendedtriggerjobs
        description: Cancel All Suspended Jobs for a Specific Trigger
        call: windmill-trigger.cancelsuspendedtriggerjobs
        with:
          trigger_kind: rest.trigger_kind
          trigger_path: rest.trigger_path
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/w/{workspace}/trigger/{trigger-kind}/resume-suspended-trigger-jobs/{trigger-path}
      name: w-workspace-trigger-trigger-kind-resume-suspended-trigger-jobs-trigger-path
      description: REST surface for w-workspace-trigger-trigger_kind-resume_suspended_trigger_jobs-trigger_path.
      operations:
      - method: POST
        name: resumesuspendedtriggerjobs
        description: Resume All Suspended Jobs for a Specific Trigger
        call: windmill-trigger.resumesuspendedtriggerjobs
        with:
          trigger_kind: rest.trigger_kind
          trigger_path: rest.trigger_path
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: windmill-trigger-mcp
    port: 9090
    transport: http
    description: MCP adapter for Windmill API — trigger. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: cancel-all-suspended-jobs-specific
      description: Cancel All Suspended Jobs for a Specific Trigger
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: windmill-trigger.cancelsuspendedtriggerjobs
      with:
        trigger_kind: tools.trigger_kind
        trigger_path: tools.trigger_path
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: resume-all-suspended-jobs-specific
      description: Resume All Suspended Jobs for a Specific Trigger
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: windmill-trigger.resumesuspendedtriggerjobs
      with:
        trigger_kind: tools.trigger_kind
        trigger_path: tools.trigger_path
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
